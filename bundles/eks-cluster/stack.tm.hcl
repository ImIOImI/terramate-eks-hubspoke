stack {
  name        = "eks-cluster"
  description = "EKS cluster bundle: network + control plane + nodes + provisioning, fanned out per environment (hub in infra, spokes in dev/prd)"
  id          = "b0075742-0000-4000-8000-000000000002"
  tags        = ["default"]
}
